# Web Framework & Server
# fastapi was unpinned (">=0.100.0") -- a fresh install resolves to whatever's
# newest at build time, which was NOT what this repo had ever actually been
# tested against (the dev env had fastapi 0.104.1/starlette 0.27.0 sitting
# untouched for a long time). Upgraded for real to 0.140.0/1.3.1 and verified:
# full test suite (297 passed), a real app lifespan startup/shutdown, and real
# HTTP requests through the full ASGI stack (health check, OpenAPI generation
# -- 441 paths, /docs, an OAuth2 form-login route, a 404) via TestClient, all
# against this exact pin. Pinned exactly so prod runs what's tested, not
# whatever floats in on the next Docker build.
fastapi==0.140.0
starlette==1.3.1
uvicorn[standard]>=0.20.0
gunicorn
python-multipart>=0.0.6

# Database
sqlalchemy>=1.4.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# Google OAuth
google-auth>=2.16.0
google-auth-oauthlib>=1.0.0
firebase-admin>=6.5.0
requests>=2.28.0

# AI/ML - Groq
groq>=0.4.0

# AI/ML - Google Gemini (Primary)
# google-generativeai is EOL (Google ended all support/updates for it);
# replaced with the actively maintained google-genai SDK's Client API
# (google.genai.Client(...).models.generate_content(...)). Was capped below
# 1.5.0 while fastapi was still 0.104.1 (newer google-genai needs anyio>=4.8,
# incompatible with fastapi 0.104.1's anyio<4.0 pin) -- that ceiling is gone
# now that fastapi is upgraded (see note above); verified 2.14.0 resolves and
# runs clean against the current fastapi/pydantic pins.
google-genai==2.14.0
# Pinned below 2.29.0: that release added a startup check that scans every
# installed package's file list, causing multi-minute import hangs on Windows.
google-api-core<2.29.0

# LangGraph Agent Framework
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# Observability (optional - for LangSmith tracing)
langsmith>=0.1.0

# AI/ML - Transformers (CPU-only torch for containerized deployment)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
transformers>=4.40.0
sentence-transformers>=2.7.0
datasets>=2.19.0
numpy>=1.26.0
scipy>=1.13.0

# Spaced Repetition - FSRS (Free Spaced Repetition Scheduler)
# Pinned <4.0 deliberately: services/fsrs_scheduler.py is written against the
# pre-rewrite API (FSRS class, Card.reps/lapses/scheduled_days/get_retrievability).
# fsrs 4.x-6.x renamed FSRS->Scheduler and replaced reps/lapses with a single
# `step` counter -- installing >=6.0.0 as previously pinned here breaks every
# flashcard spaced-repetition review with ImportError/AttributeError. Bumping
# past 3.x requires rewriting fsrs_scheduler.py against the new Card/Scheduler
# API, not just changing this pin.
fsrs>=3.1.0,<4.0.0

# Data Processing
pandas>=1.5.0
scikit-learn>=1.3.0
joblib>=1.3.0

# PDF Processing
PyPDF2>=3.0.0
PyMuPDF>=1.23.0
pymupdf4llm>=0.3.0
pdfplumber>=0.10.0
python-docx>=0.8.11
python-pptx>=0.6.21
Pillow>=10.0.0
pytesseract>=0.3.10

# PDF Generation
reportlab>=4.0.0

# Media Processing
yt-dlp>=2025.1.15
youtube-transcript-api>=0.6.0
pydub>=0.25.1

moviepy>=1.0.3
assemblyai>=0.17.0
openai-whisper>=20230314
ffmpeg-python>=0.2.0

# Language Detection
langdetect>=1.0.9
pycountry>=22.3.5

# Environment & Config
python-dotenv>=1.0.0

# Validation
# Was capped below 2.10 while fastapi was still 0.104.1 (which crashed on
# route collection under pydantic>=2.10 -- see the fastapi note above); gone
# now that fastapi is upgraded. Pinned exactly to what's tested.
pydantic[email]==2.13.4
email-validator>=2.0.0

# Caching
redis>=5.0.0
redisvl>=0.3.0

# RL Reward Scheduler
apscheduler>=3.10.0

# Utilities
aiofiles>=23.0.0
aiohttp>=3.9.0
psutil>=5.9.0
python-dateutil>=2.8.0
argon2-cffi>=23.1.0
boto3>=1.34.0
